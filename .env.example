# Root-level overrides consumed by docker-compose.yml.
# Copy to `.env` to change them; every value below is also the compose default,
# so `docker compose up -d` works with no .env file at all.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fanboard
POSTGRES_PORT=5432
REDIS_PORT=6379
